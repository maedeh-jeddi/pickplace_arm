cmake_minimum_required(VERSION 3.8)
project(pickplace_arm_description)

find_package(ament_cmake REQUIRED)

install(
  DIRECTORY urdf meshes launch rviz config worlds models
  DESTINATION share/${PROJECT_NAME}
)

ament_package()
